{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "cz-lerna-changelog": "^1.2.1",
    "lerna": "^3.4.0"
  },
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build",
    "test": "lerna run test",
    "publish": "lerna run publish"
  }
}
