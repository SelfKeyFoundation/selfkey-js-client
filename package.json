{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.4.0"
  },
  "scripts": {
    "start": "lerna run start --stream --parallel",
    "build": "lerna run build",
    "test": "lerna run test",
    "publish": "lerna run publish"
  }
}
